spring:
  application:
    name: Samdi_Tycoon_Project

  datasource:
    url: jdbc:mysql://localhost:3306/????
    username: Null
    password: Null
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
    show-sql: true

server:
  port: 8080
